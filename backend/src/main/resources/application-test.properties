spring.application.name=myStatsLolBackendTest
spring.datasource.url=jdbc:postgresql://localhost:5433/${TEST_POSTGRES_DB}
spring.datasource.username=${TEST_POSTGRES_USER}
spring.datasource.password=${TEST_POSTGRES_PASSWORD}